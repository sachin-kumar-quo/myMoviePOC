import {StyleSheet} from 'react-native'
export default styles = StyleSheet.create({
    heading: {
        fontSize: 50,
        textAlign: 'center',
        color: 'white',
        fontWeight: 'bold'
    },
    gradient: {
        flex:1
    }
})